name: Deploy to ForestLee

on:
  push:
    branches: [development, main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: forestlee-iinfo-dx-deploy
  cancel-in-progress: false

jobs:
  deploy:
    if: github.ref_name == 'development' || github.ref_name == 'main'
    runs-on: ubuntu-latest
    environment: ${{ github.ref_name == 'main' && 'production' || 'development' }}
    timeout-minutes: 45
    steps:
      - name: Join Tailscale
        uses: tailscale/github-action@v4
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci
          ping: 100.73.242.113

      - name: Clone and deploy exact commit
        env:
          DEPLOY_BRANCH: ${{ github.ref_name }}
          DEPLOY_SHA: ${{ github.sha }}
        run: |
          ssh -o BatchMode=yes -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null forestlee@100.73.242.113 \
            bash -s -- "$DEPLOY_BRANCH" "$DEPLOY_SHA" <<'REMOTE'
          set -euo pipefail
          branch=$1
          sha=$2
          [[ $branch == development || $branch == main ]]
          [[ $sha =~ ^[0-9a-f]{40}$ ]]
          root=/home/forestlee/deploy/iinfo-dx-backend
          mkdir -p "$root"
          if [[ ! -d $root/repo/.git ]]; then
            git clone https://github.com/ForestLee0513/iinfo-dx-backend.git "$root/repo"
          fi
          git -C "$root/repo" fetch --no-tags origin "$branch"
          git -C "$root/repo" checkout --force --detach "$sha"
          bash "$root/repo/deploy/deploy.sh" "$branch" "$sha"
          REMOTE
